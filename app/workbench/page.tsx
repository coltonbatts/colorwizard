import CoreWorkbench from '@/components/workbench/CoreWorkbench'

/** The full painter workbench, kept intact while the simple version leads at `/`. */
export default function WorkbenchPage() {
  return <CoreWorkbench />
}
